import { Index } from "../types/index.types";

const index: Index = {
  message: "API GROQ IA - CHAT",
  author: "Sergio Devs",
  version: "1.0.0",
  date: new Date(),
};

const service = {
  index,
};

export default service;
